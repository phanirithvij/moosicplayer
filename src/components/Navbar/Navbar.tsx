import React from 'react';

import "./Navbar.scss";

const NavbarLeft = (_props:{}) => {
    return (
        <div className="navbar_left">
            <nav className="navBar">
                
            </nav>
        </div>
    );
};

export default NavbarLeft;
